## Packages
date-fns | Formatting relative times for posts and comments
embla-carousel-react | Required for the photo carousel

## Notes
- Images are handled via URLs provided by the user (no direct file upload implemented in this slice, assumes users paste image links).
- Authentication uses Replit Auth via `/api/login`.
- Optimistic UI updates are used for likes and favorites.
